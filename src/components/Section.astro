---
const { title } = Astro.props
---

<section>
  {title && <h2>{title}</h2>}
  <slot />
</section>

<style>
  section {
    margin: 0 auto 48px;
  }

  h2 {
    font-weight: 700;
    line-height: 1.5;
    font-size: 1.5rem;
    margin-bottom: 8px;
  }

  @media (width <= 700px) {
    section {
      margin-bottom: 38px;
    }
  }
</style>
